# Required
numpy>=1.24

# Audio playback
sounddevice>=0.4

# Audio decoding (MP3 → PCM, wraps ffmpeg)
pydub>=0.25

# HTTP
requests>=2.28

# Optional: better ASCII art output
# aalib          # system package: apt install python3-aalib
# Pillow>=9.0    # needed only if aalib backend is used
